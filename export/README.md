Export your RNBO web bundle into this directory.

This folder is treated as disposable output:

- re-export into it whenever the DSP changes
- keep your hand-authored UI work outside this folder
- run `npm run sync-export` if the export filename changes
